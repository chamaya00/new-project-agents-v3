{
  "$comment": "What this repository took from the factory, and when. /update-agents reads the version here, compares it against the factory release you name, and proposes the difference as a pull request. Nothing reads this at run time - the roles in .claude/agents/, the skills in .claude/skills/, and the commands in .claude/commands/ are the real thing. This is the record of where they came from.",
  "version": "v1.18.0",
  "roles": ["orchestrator", "researcher", "designer", "engineer"],
  "skills": ["house-rules", "memory-protocol", "acceptance-criteria"],
  "commands": ["retro", "decompose", "update-agents", "ship"]
}
