# new-project-agents-v3

A scratch repository for exercising the agent factory's provisioning, labels,
and agent runs end to end.

Provisioned from agent-factory v1.7.0. See `CLAUDE.md` for how work moves here.
